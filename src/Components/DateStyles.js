import styled from "styled-components";

export const DateContainer = styled.div `
    right: 0;
    top: 5px;
    font-size: 20px;
    font-weight: 500;
    position: absolute;
    padding: 0 1rem;
`